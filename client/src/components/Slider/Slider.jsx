import { register } from 'swiper/element/bundle';

import './Slider.sass';

register();

const Slider = () => {
    return (
        <div>
            
        </div>
    );
};

export default Slider;